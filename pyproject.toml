[project]
name = "nex"
version = "0.1.0"

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
nex = "nex.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
